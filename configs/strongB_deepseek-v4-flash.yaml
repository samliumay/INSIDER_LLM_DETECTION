# Strengthened-B test (user request 2026-08-31): does stronger, log-gated monitoring
# pressure induce hiding? Ladder A < B < C < D on the harm-rich blackmail condition.
# deepseek-v4-flash via Ollama at temp 0.7 (loop-prone above; 24k cap + retries).
model: "deepseek-v4-flash:cloud"
base_url: "http://localhost:11434/v1"
temperature: 0.7
max_tokens: 24000
samples_per_condition: 5
variants: ["A", "B", "C", "D"]
conditions: ["blackmail_explicit-america_replacement"]
concurrency: 3
retries: 2
seed: 0
benchmark_dir: "../INSIDER_LLM_DETECTION_BENCHMARK"
